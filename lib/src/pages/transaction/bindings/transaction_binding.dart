import 'package:get/get.dart';

class TransactionBinding extends Bindings {
  @override
  void dependencies() {
    // Bind Booking controller
   // Get.lazyPut<HomeController>(() => HomeController(), fenix: true);
  }
}
